# 给 DeepSeek / Trae 的 K230 道路循迹开发提示词

下面整段内容可直接复制到 Trae 中交给 DeepSeek。请让 DeepSeek 在当前项目目录内工作，不要只回答方案，必须完成代码、文档和可执行检查。

---

## 主提示词

你现在负责完成一个 K230 / CanMV MicroPython 车载道路视觉项目。请以当前工作区中的实际文件和接收端源码为准，完成可部署的 K230 道路循迹视觉程序。

### 一、开始前必须读取的资料

先完整读取以下文件，不得只看文件名或摘要：

1. `视觉循迹/我chovy.txt`
   - 这是任务职责、功能范围和系统分工的最高优先级需求文档。
2. `视觉循迹/K230_现有项目硬件与编程使用手册.md`
   - 只复用其中与 CanMV v1.4.3、摄像头、Display、IDE 图传、MediaManager、FPIOA、UART、内存和异常清理有关的规则。
3. 工作区内现有 K230 程序。
   - 查找 `main_k230*.py`、`main.py`、`README.md` 和协议说明，复用已经实机使用的摄像头及 UART 初始化方式。
4. 工作区内 MSPM0 接收端源码。
   - 必须查找真正解析 K230 数据的 `.c/.h` 文件、UART 配置和 CRC 实现。
   - 实际接收解析器优先级高于 README、旧协议草案和注释。

开始编码前，先输出一份不超过一页的“需求与资源审计”，列出：

- 找到的权威文件；
- K230 固件和摄像头型号；
- 摄像头 Sensor ID、输出通道、图像格式和分辨率；
- UART 实例、物理引脚、波特率和接收端解析器位置；
- 当前协议是否能容纳道路循迹数据；
- 哪些内容仍缺少实物或接收端证据；
- 准备创建和修改哪些文件。

完成审计后继续实现，不要只停留在方案讨论。

### 二、任务职责边界

本项目是车载黑线道路视觉，不是 D36A 云台或激光矩形追踪。

K230只负责：

- 采集前向摄像头图像；
- 裁剪道路 ROI；
- 提取左右黑色道路边界；
- 计算道路中心线；
- 计算横向偏差；
- 计算航向偏差；
- 判断左边界、右边界是否有效；
- 在单边界情况下短时估算道路中心并降低可信度；
- 检测左支路、右支路、十字/扩宽区域、道路终点或房间入口候选；
- 估计路口相对位置或距离等级；
- 进行多帧确认、可信度评估和异常检测；
- 根据 MSPM0 命令切换 IDLE、NUMBER、TRACK 等视觉模式；
- 把结构化视觉结果通过 UART 发送给 MSPM0。

K230不负责：

- 电机 PWM；
- D36A STEP/DIR；
- 左右轮速度闭环；
- 编码器处理；
- 决定当前路口左转还是右转；
- 房间路线规划；
- 90°、180°转弯动作；
- 到达房间、返回药房等总任务状态机；
- 激光 GPIO35 控制；
- 矩形中心减激光中心的云台闭环。

不得从硬件指南中复制以下无关内容到本项目：

- GPIO42/26、GPIO43/34 的 D36A 电机控制；
- `StepperAxis`；
- PWM0/PWM1 双轴控制；
- GPIO35 激光控制；
- `virtual_steps`；
- 矩形—激光误差控制器。

如果发现已有代码包含这些功能，只能将其当作摄像头、显示、UART 生命周期的参考，不得合入道路循迹主程序。

### 三、来源优先级

遇到冲突时按以下优先级处理：

1. `我chovy.txt`：决定任务职责和功能边界；
2. MSPM0 实际接收解析器：决定 UART 帧格式、字节序、CRC 和合法字段；
3. 实板已经运行的 K230 代码：决定 Sensor、Display、FPIOA、UART 的板卡兼容写法；
4. `K230_现有项目硬件与编程使用手册.md`：决定 CanMV 生命周期和安全约束；
5. README、旧协议草案和通用示例。

不得为了快速完成而自行编造引脚、摄像头方向、道路尺寸、协议字段单位或接收端行为。

### 四、建议的输出目录和文件

在 `视觉循迹` 下创建清晰、可查找的项目目录，例如：

```text
视觉循迹/
├─ k230_road_vision/
│  ├─ main_k230_road_vision.py
│  ├─ road_config.py
│  ├─ road_geometry.py
│  ├─ road_structure.py
│  ├─ vision_protocol.py
│  ├─ README.md
│  ├─ CALIBRATION.md
│  └─ tests/
│     ├─ test_road_geometry.py
│     └─ test_vision_protocol.py
├─ 我chovy.txt
└─ K230_现有项目硬件与编程使用手册.md
```

如果 CanMV 板端不适合多文件导入，可以额外生成一个合并部署文件：

```text
k230_road_vision/main.py
```

模块化源码和合并版必须保持同一逻辑，不得出现两个版本参数不一致。

不要删除现有资料，不要修改无关仓库。

### 五、摄像头和媒体生命周期

必须遵守 CanMV v1.4.3 实际接口。

基本顺序：

```text
创建安全状态的通信对象
→ 创建 Sensor
→ sensor.reset()
→ 配置活跃输出通道的分辨率和像素格式
→ 可选 Display.init()
→ MediaManager.init()
→ sensor.run()
→ 等待曝光稳定并预热
→ 主循环
```

退出顺序：

```text
停止发送有效控制数据或发送视觉无效状态
→ sensor.stop()
→ Display.deinit()
→ os.exitpoint(os.EXITPOINT_ENABLE_SLEEP)
→ MediaManager.deinit()
→ UART deinit
```

要求：

- 不要无依据启用双摄像头输出通道。道路循迹只需一个通道时就使用一个通道。
- 如果启用多个 `CAM_CHN_ID`，每个活跃通道必须持续 `snapshot()`，防止 VB 队列积满。
- 不能在主循环里重复创建 Sensor、FPIOA、UART 或 Display。
- `Display.init()` 必须在 `MediaManager.init()` 前。
- `Display.deinit()` 必须在 `MediaManager.deinit()` 前。
- `to_ide=True` 仅用于短时调试，正式运行默认关闭或降低刷新频率。
- 不允许使用 Python 双重逐像素循环处理完整图像；优先使用 CanMV `image` API、`cv_lite`、`ulab` 或行采样。
- 避免每帧创建大型列表、字符串和图像副本。
- `to_numpy_ref()` 只在当前帧内使用，不能跨帧保存引用。

### 六、道路视觉算法要求

先实现稳定的基础道路循迹，再增加路口识别。不要一次堆入未经验证的复杂算法。

#### 1. 图像预处理

实现可配置的：

- 道路 ROI，排除车体自身和无关画面；
- 灰度或适合黑线的颜色空间；
- 黑线阈值；
- 轻量去噪；
- 必要时的透视/鸟瞰变换；
- 曝光异常、过暗、过亮和模糊检测。

所有参数集中放在配置区，注明单位、坐标系和合法范围。

#### 2. 边界提取

沿多个从近到远的采样行提取：

- 左边界点；
- 右边界点；
- 每个采样行的道路中心；
- 道路宽度；
- 边界连续性；
- 有效点数量。

不能简单选择“全画面最大黑块”作为道路。

必须避免：

- 车体黑色部件；
- 地面污渍；
- 阴影；
- 门口黑白虚线；
- 画面边缘无关黑色结构；
- 单帧突然出现的远处假边界。

#### 3. 道路几何

输出至少包括：

- `lateral_error`：横向偏差；
- `heading_error`：航向偏差；
- `road_width`；
- `left_valid`；
- `right_valid`；
- `geometry_valid`。

横向偏差和航向偏差的正负方向必须在代码和 README 中明确，并提供至少三个示例：

- 道路中心在车体右侧；
- 道路中心在车体左侧；
- 道路向右或向左倾斜。

使用统一的车辆坐标系或归一化坐标。不要让 MSPM0 依赖摄像头像素分辨率。

#### 4. 单边界降级

双边界有效时更新历史道路宽度。

只有一侧边界时：

- 使用可信的历史道路宽度短时估算另一侧；
- 设置 `degraded=1`；
- 降低可信度；
- 禁止确认新路口；
- 超过限定帧数或时间后转为无效。

双边界丢失时：

- 不得把旧偏差伪装成新测量；
- 输出 `vision_valid=0`；
- 由 MSPM0 执行减速或停车。

#### 5. 支路和路口

路口判断必须综合多个特征：

- 道路宽度增大；
- 左/右边界向外延伸；
- 远处出现新的连通道路区域；
- 连续多帧结构一致；
- 结构位置随车辆运动逐渐接近。

输出“看到什么”，例如：

```text
left_branch=1
right_branch=0
intersection_candidate=1
junction_stage=APPROACHING
```

不得输出：

```text
TURN_LEFT
TURN_RIGHT
```

路线选择属于 MSPM0。

路口事件必须多帧确认并具有进入/离开迟滞，避免同一路口每帧重复触发。

#### 6. 可信度

可信度至少综合：

- 左右边界有效点数量；
- 拟合残差；
- 道路宽度合理性；
- 边界连续性；
- 与上一帧结果的变化；
- 图像亮度/模糊度；
- 路口结构稳定性。

定义三类视觉状态：

```text
NORMAL
DEGRADED
INVALID
```

不能只用“检测到了某个黑色物体”作为高可信度。

### 七、运行模式

实现明确的模式状态机：

```text
IDLE
NUMBER
TRACK
TURNING
REACQUIRE
FAULT
```

最低要求：

- `IDLE`：低频或暂停道路分析，等待 MSPM0 命令；
- `TRACK`：持续道路边界、偏差和路口检测；
- `TURNING`：MSPM0 正在执行转弯，K230 可以继续采图，但发送结果必须标记暂不可用于正常循迹；
- `REACQUIRE`：转弯结束后重新寻找稳定道路，连续多帧有效后才报告恢复；
- `FAULT`：摄像头、媒体或算法异常，发送视觉无效。

`NUMBER` 第一阶段可以保留接口但默认关闭。若要复用数字识别，必须使用现有模型和现有锁定/解锁协议，不得把数字识别强行混进每帧道路循环。

### 八、UART 与 MSPM0 协议

不要直接复用矩形/激光项目的 22 字节观测帧，因为其字段不包含道路宽度、横向偏差、航向偏差和支路信息。

必须先找到 MSPM0 的实际接收解析器：

- 若接收端已经定义道路视觉帧，K230 必须逐字节匹配；
- 若只有旧矩形/激光协议，先明确报告“不兼容”，再设计版本化道路协议；
- 如果工作区中包含 MSPM0 工程，并且用户允许修改该工程，则同步实现接收解析器、字段检查和错误计数；
- 如果找不到接收端源码，不得声称通信已联调完成。

道路视觉帧至少需要表达：

- 帧头和协议版本；
- 消息类型；
- 单调递增帧序号；
- 采集时间戳或帧时间；
- 当前模式；
- `vision_valid`；
- `degraded`；
- `lateral_error`；
- `heading_error`；
- `road_width`；
- `left_valid`、`right_valid`；
- `left_branch`、`right_branch`；
- 路口阶段或结构标志；
- 路口距离/距离等级；
- 可信度；
- 异常标志；
- CRC。

协议要求：

- 明确帧长；
- 明确每个字段的偏移、类型、单位、范围和字节序；
- 明确 CRC 算法和覆盖范围；
- 无效值必须有唯一表示；
- 接收端必须拒绝错误帧长、非法字段、CRC 错误和重复/倒序序号；
- K230 使用预分配 `bytearray`，避免每帧字符串格式化；
- 高频正式链路使用二进制帧；
- ASCII 调试日志不得混入二进制 UART；
- 若保留终端状态输出，只低频打印状态、FPS、可信度和关键错误，不输出每帧垃圾日志。

接收端验证至少包含计数器：

```text
validFrames
crcErrors
lengthErrors
fieldErrors
sequenceErrors
overflowErrors
```

波特率、8N1、TX/RX 交叉、3.3V TTL 和共地必须两端一致。

### 九、必须修复或避免的已知错误

不要生成以下错误：

1. 使用 `CAM_WIDTH / DETECT_W` 把 320×240 检测坐标错误缩放到 1280×960，却又减去 640×480 中心 `(320,240)`。
2. 把检测坐标、显示坐标、逻辑坐标和车辆坐标直接混算。
3. 在 `detect` 模式仍然驱动电机。
4. 声称存在 ACQUIRE/HOLD 状态，但控制器只使用单帧 `detected`，导致未确认就输出、漏一帧就归零。
5. 写出点动命令但只 `sleep`，没有持续更新执行器。
6. 用控制周期时间计算 FPS。
7. 把目标中心坐标当成误差打印。
8. 手工写静态“验证器”，却没有读取源码就无条件输出全部通过。
9. 把 `digit_cnn_int8.kmodel` 和 `digit_glare_int8.kmodel` 对应关系写反。
10. 把道路循迹任务误写成 D36A 云台追踪。

### 十、测试要求

PC 测试只覆盖纯算法和协议，不得导入 `machine`、`media` 等板端模块。

至少实现以下测试：

#### 道路几何测试

- 双边界居中直路：横向偏差约为 0，航向偏差约为 0；
- 道路整体右移：横向偏差符号正确；
- 道路整体左移：横向偏差符号正确；
- 道路向右倾斜和向左倾斜：航向偏差符号正确；
- 单左边界：进入 DEGRADED，使用历史宽度估算；
- 单右边界：进入 DEGRADED；
- 双边界丢失：输出 INVALID；
- 道路宽度异常：降低可信度；
- 单帧跳变：不立即接管历史结果。

#### 路口测试

- 普通直路不误报支路；
- 左支路只置 `left_branch`；
- 右支路只置 `right_branch`；
- 十字路口多特征一致；
- 路口需连续多帧确认；
- 同一路口不会每帧重复触发；
- 转弯期间结果标记不可用于正常循迹；
- 转弯结束需稳定多帧才报告 REACQUIRED。

#### 协议测试

- 正常帧编码后由接收端解码得到完全相同字段；
- CRC 标准向量；
- 单字节破坏后 CRC 必须失败；
- 截断帧、超长帧、非法字段被拒绝；
- 序号重复和倒序被识别；
- 无效测量不会被编码成正常零误差。

### 十一、板端验证步骤

代码完成后给出逐阶段实板验证，不得一上来就让车辆自动运行：

1. 只开摄像头和低频显示，不接电机，确认画面方向和 ROI；
2. 显示二值图、采样行、左右边界、中心线；
3. 手动移动道路板，验证横向/航向偏差正负；
4. 遮挡左边界、右边界和双边界，验证 NORMAL/DEGRADED/INVALID；
5. 手动通过左支路、右支路和十字结构，验证多帧事件；
6. 用串口工具检查二进制帧、CRC、序号和发送频率；
7. 连接 MSPM0，但先不驱动电机，确认接收计数器；
8. 低速直线闭环；
9. 低速单弯道；
10. 最后才测试路口和任务流程。

每一步失败时只修改一个层次，不要同时改视觉、协议和下位机控制。

### 十二、性能和安全指标

最低目标：

- 不开 IDE 图传时持续稳定运行；
- 视觉循环和发送频率有实测统计；
- 无持续内存下降；
- 无 `snapshot failed(3)`；
- UART 不阻塞视觉循环；
- 正常直路不频繁跳入 INVALID；
- 丢失道路时发送无效状态，不发送伪造的零误差；
- 所有异常退出都能释放 Sensor、Display、MediaManager 和 UART。

不得把 PC 语法检查、离线图片测试或模拟器结果描述为 K230 实板验证。

### 十三、每次回复的固定格式

每完成一个阶段，按以下格式汇报：

```text
本阶段结果：
- 已完成：
- 尚未完成：

修改文件：
- 文件：
- 函数/类：
- 修改目的：

保持不变：
- 硬件：
- 协议：
- 其他稳定逻辑：

验证：
- 已运行的命令：
- 通过项：
- 失败项：
- 仍需K230实板验证：

下一步：
- 只列最直接的下一阶段。
```

不要只给建议或伪代码。最终必须交付可运行源码、配置说明、协议说明、PC 纯算法测试和明确的实板验证清单。

### 十四、最终验收

只有满足以下条件才能称为“代码实现完成”：

- 板端主程序具有正确的 Sensor/Media 生命周期；
- 道路 ROI、左右边界、中心线、横向偏差、航向偏差已实现；
- 单边界降级、双边界失效已实现；
- 支路/路口具有多帧确认；
- 可信度和异常标志已实现；
- IDLE/TRACK/TURNING/REACQUIRE/FAULT 模式可切换；
- 二进制协议与实际 MSPM0 接收器一致；
- 纯算法和协议测试通过；
- 代码能通过语法检查；
- README 和 CALIBRATION 写明使用方法；
- 未加入 D36A、电机 PWM、激光或路线决策；
- 明确列出仍需实板验证的内容。

只有完成实际 K230 + MSPM0 联调并提供计数器、FPS 和道路场景结果后，才能称为“硬件验证完成”。

---

## 建议在 Trae 中的第一句话

将上面的主提示词发给 DeepSeek 后，再补充这一句：

```text
现在先执行资料和源码审计，然后直接开始第一阶段实现。不要修改任务职责边界；
如果找不到 MSPM0 实际接收解析器，请先完成道路视觉算法和版本化协议草案，
明确标记“接收端未确认”，不要假装已经联调。
```

