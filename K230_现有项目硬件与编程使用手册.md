# K230 现有项目硬件与编程使用手册

> 适用范围：本仓库的 K230 / CanMV MicroPython 视觉、数字识别、UART 通信、D36A 双轴步进和激光检测代码。  
> 固定环境：CanMV v1.4.3、MicroPython e00a144、GC2093 CSI2 摄像头。  
> 编写目的：把本文件交给其他智能体作为开发前置约束，避免混淆物理引脚、FPIOA 功能、摄像头输出通道、PWM 通道和显示通道。

## 1. 先确定代码基线

仓库中有多代程序，不能把不同版本的局部写法随意拼接。

| 用途 | 优先参考文件 | 特点 |
|---|---|---|
| 完整矩形－激光－双轴闭环 | `main_k230_d36a_laser_rect_center_track_v10_11_smooth_motion_tune.py` | 双摄像头输出通道、LCD 可选、UART3、双轴 STEP/DIR、GPIO35 激光 |
| 纯视觉节点向 MSPM0 发观测量 | `main_k230_vision_uart_plan_b_v1.py` | 无电机控制、22 字节二进制帧、460800 baud |
| 固定左右槽数字识别 | `main_k230_digit_redline_lr.py` | `PipeLine`、KPU、LCD、默认关闭 IDE 图传 |
| 单区域数字识别与锁定握手 | `main_k230_digit_lenet_uart.py` | `PipeLine`、KPU、UART3、IDE 图传 |
| PWM 显式映射参考 | `main_k230_d36a_laser_target_ibvs_v9_4_1_pwm_api_compat.py` | GPIO→PWM 通道显式 FPIOA 映射 |

其他智能体开始工作前必须先说明：

1. 要修改哪一个文件和哪一层功能。
2. 哪些行为必须保持不变。
3. 是否允许改接线、波特率、数据包或显示方式。
4. 只修改指定层，不得顺手重写已稳定的识别、状态机和协议。

## 2. 必须分清的五类“编号”

以下编号属于完全不同的命名空间，数值相同也没有继承关系。

| 类型 | 示例 | 实际含义 |
|---|---|---|
| 物理 IO/PAD 编号 | GPIO32、GPIO42 | 开发板引出的物理管脚编号 |
| FPIOA 功能 | `FPIOA.UART3_TXD`、`FPIOA.PWM0` | 把某个内部外设功能路由到物理 PAD |
| 外设实例/通道 | UART3、PWM0、PWM1 | K230 内部硬件外设资源 |
| 摄像头输出通道 | `CAM_CHN_ID_0/1/2` | 同一个 Sensor/ISP 的并行图像输出通道 |
| 显示图层 | `LAYER_VIDEO1`、`LAYER_OSD0` | Display 内部视频或 OSD 图层 |

错误示例：

- `CAM_CHN_ID_1` 不是“第二个摄像头”。
- GPIO43 不等于 PWM43；GPIO43 在当前复用表中对应 PWM1。
- UART3 不等于 GPIO3；UART3 当前被映射到 GPIO32/33。
- `Sensor(id=2)` 中的 `2` 不是 `CAM_CHN_ID_2`。

## 3. 当前硬件与固定接线

### 3.1 主要硬件

- K230 CanMV 开发板。
- GC2093 CSI2 摄像头。
- ST7701 MIPI 屏，当前代码按 640×480 初始化。
- D36A 双路步进驱动板。
- 两相步进电机二维云台。
- 3.3 V USB-TTL，或使用 3.3 V TTL 的 MSPM0。
- 带 TTL/EN 输入的激光模块及独立供电。

### 3.2 当前接线表

| 功能 | K230 物理 IO | 内部功能/对象 | 外部连接 |
|---|---:|---|---|
| X STEP | GPIO42 | PWM0 可选 PAD | D36A STEP1 |
| X DIR | GPIO26 | GPIO26 输出 | D36A DIR1 |
| Y STEP | GPIO43 | PWM1 可选 PAD | D36A STEP2 |
| Y DIR | GPIO34 | GPIO34 输出 | D36A DIR2 |
| UART3 TX | GPIO32 | `FPIOA.UART3_TXD` | USB-TTL/MSPM0 RX |
| UART3 RX | GPIO33 | `FPIOA.UART3_RXD` | USB-TTL/MSPM0 TX |
| 激光 TTL/EN | GPIO35 | GPIO35 输出 | 激光驱动的逻辑控制端 |
| 公共地 | GND | — | K230、D36A、TTL、激光驱动共地 |

### 3.3 电气安全

- K230 GPIO 是逻辑控制端，不是电源输出端。
- 不得把 D36A 的 5 V 接入任何 K230 GPIO。
- USB-TTL 只接 TX、RX、GND，不接其 5 V/VCC。
- D36A EN1/EN2 仍按现有硬件接到驱动板自身 5 V，代码中保持：

```python
USE_COMMON_ENABLE_PIN = False
```

- GPIO35 已用于激光 TTL，不能同时作为 D36A 公共使能。
- 激光必须使用独立合规供电和驱动，GPIO35 只提供高低电平。
- 电机、激光和 K230 必须共地，但动力电源不要从 K230 GPIO 取电。

## 4. FPIOA 与普通 GPIO

### 4.1 FPIOA 的作用

K230 的物理 PAD 可以复用为 GPIO、UART、PWM 等功能。使用外设前，必须确认同一物理 PAD 没有被其他功能占用。

普通输出的现有兼容写法：

```python
from machine import FPIOA, Pin

pin_number = 35
fpioa = FPIOA()
fpioa.set_function(
    pin_number,
    FPIOA.GPIO35,
    ie=0,
    oe=1,
    pu=0,
    pd=0,
)
laser_pin = Pin(pin_number, Pin.OUT, drive=7)
laser_pin.value(0)
```

仓库中的 `make_gpio_output()` 包含旧/新构造器兼容回退，应优先复用，不要在同一个程序里再写一套不同的 GPIO 初始化。

### 4.2 输出控制规则

- 创建输出后立刻写安全初值。
- 激光默认 OFF。
- STEP PWM 默认 duty=0。
- 改 DIR 前先停止 STEP，再等待 `DIR_SETUP_US`。
- 程序退出、异常或 Ctrl+C 时必须关闭 PWM 和激光。
- 不要在普通循环内重复创建 `FPIOA()`、`Pin()` 或 `PWM()`。

### 4.3 排查复用冲突

在板端可用以下查询方式确认映射：

```python
fpioa = FPIOA()
fpioa.help(32)
fpioa.help(33)
fpioa.help(35)
fpioa.help(42)
fpioa.help(43)
```

其他智能体若要新增 I2C、SPI、额外 UART 或 PWM，必须先列出现有占用表，并确认不会覆盖 GPIO26/32/33/34/35/42/43。

## 5. 摄像头、ISP 与内部图像通道

### 5.1 当前摄像头结构

当前代码使用：

```python
SENSOR_ID = 2
sensor = Sensor(
    id=SENSOR_ID,
    width=1280,
    height=960,
    fps=90,
)
```

这里表示使用 sensor 设备 2 对应的 GC2093 输入模式。`Sensor(id=2)` 后必须调用：

```python
sensor.reset()
```

一个 Sensor/ISP 最多可并行输出三个通道：

```text
GC2093 / Sensor(id=2)
        |
        +-- CAM_CHN_ID_0：矩形/主视觉
        +-- CAM_CHN_ID_1：激光颜色检测
        +-- CAM_CHN_ID_2：当前项目未使用
```

### 5.2 当前双通道分配

完整闭环 v10.11：

| 通道 | 尺寸 | 像素格式 | 用途 |
|---|---:|---|---|
| `CAM_CHN_ID_0` | 320×240 | `Sensor.RGB888` | `cv_lite` 矩形检测、`to_numpy_ref()` |
| `CAM_CHN_ID_1` | 320×240 | `Sensor.RGB565` | `find_blobs()` 激光检测 |

Plan B 视觉节点：

| 通道 | 尺寸 | 像素格式 | 用途 |
|---|---:|---|---|
| `CAM_CHN_ID_0` | 320×240 | `Sensor.RGB888` | 矩形 |
| `CAM_CHN_ID_1` | 160×120 | `Sensor.RGB565` | 高频激光 |

配置顺序：

```python
sensor.reset()
sensor.set_framesize(width=320, height=240, chn=CAM_CHN_ID_0)
sensor.set_pixformat(Sensor.RGB888, chn=CAM_CHN_ID_0)
sensor.set_framesize(width=320, height=240, chn=CAM_CHN_ID_1)
sensor.set_pixformat(Sensor.RGB565, chn=CAM_CHN_ID_1)
```

### 5.3 像素格式不能随意替换

- `RGB888` 供矩形检测和 NumPy 引用使用。
- `RGB565` 是当前 `find_blobs()` 激光颜色/LAB 路线的可靠通道。
- 不要为了“统一格式”把激光通道改成 RGB888。
- 不要假设两个格式的阈值、颜色元组或内存占用相同。
- `to_numpy_ref()` 是对当前帧缓冲的引用，不应长期保存；下一帧或缓冲复用后内容可能失效。

### 5.4 双通道必须持续取帧

双通道启用后，每个活跃通道都要持续执行：

```python
rect_img = sensor.snapshot(chn=CAM_CHN_ID_0)
laser_img = sensor.snapshot(chn=CAM_CHN_ID_1)
```

即使某一轮不执行矩形或激光算法，也要把对应通道的帧取出，使 VB 队列持续出队。只取一个通道会造成另一通道缓冲堆积，随后可能出现：

```text
snapshot failed(3)
```

可以跳过算法，不能长期跳过活跃通道的 `snapshot()`。

### 5.5 启动预热

正确顺序是：

1. 创建和 reset Sensor。
2. 配置每个通道的尺寸和格式。
3. 如需显示，先 `Display.init()`。
4. `MediaManager.init()`。
5. `sensor.run()`。
6. 等待约 300 ms。
7. 双通道各取若干帧预热。
8. 相机稳定后再打开激光并进入识别。

预热用于丢弃旧帧，并等待自动曝光趋于稳定。不要让预热帧进入目标锁定器或多帧确认器。

### 5.6 坐标系

当前项目常同时存在三种坐标：

| 坐标系 | 典型尺寸 | 用途 |
|---|---:|---|
| Sensor 输入模式 | 1280×960 | GC2093/ISP 输入能力 |
| 检测坐标 | 320×240 或 160×120 | 实际算法处理 |
| 逻辑/控制坐标 | 640×480 | UART、控制误差、显示说明 |

必须显式缩放：

```python
logical_x = detect_x * 640.0 / detect_width
logical_y = detect_y * 480.0 / detect_height
```

禁止把 160×120 激光坐标直接与 320×240 矩形坐标相减，也禁止把检测坐标直接当成 640×480 控制坐标。

### 5.7 镜像与翻转

若机械安装方向变化，可在 `sensor.run()` 前设置：

```python
sensor.set_hmirror(True)
sensor.set_vflip(True)
```

但是镜像会改变坐标和控制方向。修改后必须重新验证：

- 画面右移是否仍产生正确 X 误差。
- 画面下移是否仍产生正确 Y 误差。
- 只改 `X_REVERSE/Y_REVERSE` 或镜像中的一层，不要同时反转多处符号。

## 6. 屏幕和 CanMV IDE 图像显示

### 6.1 当前 ST7701 显示

完整闭环代码采用：

```python
Display.init(
    Display.ST7701,
    width=640,
    height=480,
    to_ide=False,
    quality=35,
)
```

含义：

- `Display.ST7701`：输出到物理 MIPI LCD。
- `to_ide=False`：不把显示内容额外压缩为 JPEG 发送到 IDE。
- `to_ide=True`：LCD 仍显示，同时把画面传给 IDE；会增加内存、压缩和通信负担。
- `quality` 只在 `to_ide=True` 时影响 IDE JPEG 质量。

### 6.2 三种推荐模式

| 目的 | 配置 | 说明 |
|---|---|---|
| 实机高性能闭环 | `ENABLE_DISPLAY=False` | 不初始化显示，抖动最小 |
| 只看物理屏 | ST7701 + `to_ide=False` | 当前调试推荐 |
| 同时看屏和 IDE | ST7701 + `to_ide=True` | 只用于短时调试，帧率可能下降 |

如只需 IDE 虚拟显示，可使用 `Display.VIRT`，但这不是当前完整闭环代码的默认路径。不得在未知硬件上把 ST7701、LT9611/HDMI 和 VIRT 名称互换。

### 6.3 显示刷新不应绑定控制频率

当前高性能做法是：

```python
if frame_count % DISPLAY_EVERY_N_FRAMES == 0:
    Display.show_image(img, x=display_x, y=display_y)
```

识别和控制可以高频运行，LCD 只每 5 帧刷新一次。不要因为屏幕只刷新约 4～18 Hz 就判断摄像头或控制只有该帧率。

### 6.4 图像居中与绘制坐标

320×240 图像显示在 640×480 画布时：

```python
display_x = (640 - 320) // 2
display_y = (480 - 240) // 2
Display.show_image(img, x=display_x, y=display_y)
```

如果直接在 `img` 上画框，绘制坐标使用该图像自己的 320×240 检测坐标；`x/y` 只决定整幅图放在显示画布的哪个位置。

### 6.5 Display 初始化与释放顺序

初始化：

```text
Sensor 配置
→ Display.init()
→ MediaManager.init()
→ sensor.run()
```

退出：

```text
停止电机和激光
→ sensor.stop()
→ Display.deinit()
→ os.exitpoint(os.EXITPOINT_ENABLE_SLEEP)
→ MediaManager.deinit()
```

`Display.init()` 必须早于 `MediaManager.init()`；`Display.deinit()` 必须晚于 `sensor.stop()`、早于 `MediaManager.deinit()`。

## 7. `PipeLine` 路线与直接 Media 路线

数字识别程序使用：

```python
from libs.PipeLine import PipeLine

pl = PipeLine(
    rgb888p_size=[320, 240],
    display_size=[640, 480],
    display_mode="lcd",
)
pl.create(fps=30, to_ide=False)
frame = pl.get_frame()
pl.show_image()
```

`PipeLine` 已封装 Sensor、Display、MediaManager 和 OSD，适合 KPU/AI 示例。直接媒体路线则显式调用 `Sensor`、`Display`、`MediaManager`。

规则：

- 一个程序选择一种生命周期管理方式。
- 使用 `PipeLine` 时，不要再重复 `Display.init()` 或 `MediaManager.init()`。
- 绘制结果通常画到 `pl.osd_img`，不是直接修改 CHW 的 `frame` NumPy 数据。
- `rgb888p_size` 对应 AI 输入帧，`display_size` 对应 OSD/显示画布，坐标需按比例换算。
- `pl.show_image()` 负责合成并显示 OSD。
- 退出时调用 `pl.destroy()`；KPU 对象释放后可调用 `nn.shrink_memory_pool()`。

## 8. MediaManager、VB 缓冲与内存

### 8.1 MediaManager 的作用

Sensor、Display 和图像缓冲依赖媒体/VB 资源。现有 v1.4.3 代码仍显式调用：

```python
MediaManager.init()
...
MediaManager.deinit()
```

不要在主循环里重复 init/deinit，也不要异常后跳过清理。

### 8.2 内存规则

- 不要每帧创建大型 Python 列表、字符串或完整图像副本。
- 优先使用 `to_numpy_ref()`，但只在当前帧内消费。
- UART 二进制包使用预分配 `bytearray`。
- 高频日志和 IDE JPEG 都会增加时序抖动。
- 定期检查 `gc.mem_free()`，仅在低于阈值或固定较长间隔时 `gc.collect()`。
- KPU 模型放在 `/sdcard/kmodel/`，代码中的路径必须与实际文件完全一致。

## 9. UART3 的使用

### 9.1 FPIOA 和构造

```python
from machine import FPIOA, UART

fpioa = FPIOA()
fpioa.set_function(32, FPIOA.UART3_TXD, ie=0, oe=1)
fpioa.set_function(33, FPIOA.UART3_RXD, ie=1, oe=0)

uart_id = getattr(UART, "UART3", 3)
uart = UART(
    uart_id,
    baudrate=115200,
    bits=8,
    parity=None,
    stop=1,
    timeout=0,
)
```

CanMV v1.4 官方通用文档写明 UART3 被大核 Shell 占用，公开列出的用户 UART
是 UART1、UART2、UART4；但本项目的 Yahboom CanMV v1.4.3 固件和现有程序
一直按 UART3、GPIO32/33 编写，项目协议测试也以这一路为基线。这说明板卡固件、
Shell 配置或厂商构建与通用文档可能存在差异。

因此：

- 不要仅根据通用文档把现有 UART3 自动“修正”为 UART1/2/4。
- 也不要仅根据现有代码断言所有 K230 v1.4 固件都允许用户占用 UART3。
- 在本板上先运行最小收发测试，并用 `fpioa.help(32/33)` 检查映射。
- 若 UART3 与 IDE/Shell 冲突，再在确认板卡引脚表和对端接线后迁移，迁移时要同时修改 FPIOA 功能、UART 实例和物理接线。

接线必须交叉：

```text
K230 TX(GPIO32) → 对端 RX
K230 RX(GPIO33) ← 对端 TX
K230 GND        ↔ 对端 GND
```

### 9.2 两种现有波特率不能混用

| 程序 | 波特率 | 数据形式 |
|---|---:|---|
| 双轴调参、数字识别 | 115200 8N1 | ASCII 方括号包 |
| Plan B → MSPM0 | 460800 8N1 | 22 字节二进制帧 |

改变 K230 波特率时，必须同步修改、重新生成、编译和烧录接收端配置。仅改发送端会造成全是乱码或 CRC 错误。

### 9.3 非阻塞接收

主循环中只做有限工作量：

```python
if uart.any():
    data = uart.read()
```

- 使用缓存处理半包和粘包。
- 限制每轮最多处理的包数。
- 不要用无限等待的 `readline()` 卡住视觉循环。
- ASCII 协议和二进制协议必须使用不同解析器，不能以 `decode()` 处理任意二进制帧。

### 9.4 Plan B 二进制协议

当前实际接收端协议为：

- 帧长 22 字节。
- 帧头 `0xAA 0x55`。
- CRC16-CCITT-FALSE。
- 多项式 `0x1021`，初值 `0xFFFF`。
- CRC 覆盖 byte 0～19。
- 无效坐标使用 `0xFFFF`。
- 460800 baud。

其他智能体不得按旧 README 的 24 字节草案改包，也不得额外发送未请求的数据包。

## 10. PWM、STEP/DIR 与内部通道

### 10.1 CanMV v1.4 支持的两种 PWM 构造

方式 A：直接传物理引脚，驱动自动设置复用：

```python
pwm_x = PWM(42, freq=100, duty=0)
```

方式 B：显式 FPIOA 映射后传通道号：

```python
fpioa = FPIOA()
fpioa.set_function(42, FPIOA.PWM0)
pwm_x = PWM(0, freq=100, duty=0)
```

在 CanMV v1.4 官方接口中两者都成立。其他智能体必须整段保持同一种风格，不能映射为 PWM0 却创建另一个通道。

### 10.2 当前引脚对应关系

| PWM 通道 | 当前或可选物理引脚 |
|---|---|
| PWM0 | GPIO42、GPIO54、GPIO60 |
| PWM1 | GPIO43、GPIO55、GPIO61 |
| PWM2 | GPIO7、GPIO46、GPIO56 |
| PWM3 | GPIO8、GPIO47、GPIO57 |
| PWM4 | GPIO9、GPIO52、GPIO58 |
| PWM5 | GPIO25、GPIO53、GPIO59 |

### 10.3 当前双轴代码的关键硬件限制

K230 有两组 PWM 时钟：

```text
PWM0、PWM1、PWM2：共享同一输出频率
PWM3、PWM4、PWM5：共享另一输出频率
```

当前 X STEP=GPIO42→PWM0，Y STEP=GPIO43→PWM1，二者在同一频率组。占空比可以独立，但频率不能真正独立。

因此以下需求彼此冲突：

```text
X 轴同时输出 320 Hz
Y 轴同时输出 180 Hz
```

如果代码依次调用 `x_pwm.freq(320)`、`y_pwm.freq(180)`，硬件组的最终频率可能被最后一次调用覆盖。对象里保存的数值不等于引脚上一定存在两种独立频率。

这是当前 v10.x 双轴连续变频方案必须上示波器/逻辑分析仪验证的风险，不能仅凭“PWM 对象创建成功”判定正确。

可选解决方向：

1. 不改接线：只允许两轴使用相同 STEP 频率，通过 duty/脉冲门控决定各轴是否走步；需要重新设计运动调度。
2. 改接线：把其中一个 STEP 改到 PWM3～PWM5 所在的另一组兼容引脚，例如 PWM3 的 GPIO8/47/57；必须先确认开发板实际引出、没有与屏幕/摄像头/启动功能冲突。
3. 使用软件定时、PIO/专用脉冲调度或下位机产生双轴独立 STEP；需要重新评估最高频率和抖动。

未经用户确认，不得自动改物理接线或批量改引脚。

### 10.4 STEP/DIR 控制约束

- STEP 输出采用约 50% duty 的方波，频率代表每秒脉冲数。
- DIR 是普通 GPIO。
- 换向时先 duty=0，再改 DIR，等待至少 `DIR_SETUP_US`，然后恢复 STEP。
- 低于电机可靠启动频率时应停止或使用最小有效频率。
- 频繁微调 `PWM.freq()` 会产生重装载和时序抖动，现有代码用绝对/相对门限减少更新。
- 程序退出必须先 `duty(0)`，再 `pwm.deinit()`。

### 10.5 软件位置不是编码器

```python
virtual_steps += output_hz * dt
```

`virtual_steps` 只是按命令积分的估计值。堵转、失步、突然断电、手动转动云台后都会失真。

- 每次上电前把云台放在机械安全中位。
- 再执行 `[motor,zero]`。
- 软限位不能代替机械限位或编码器。
- 不得把 `virtual_steps` 描述为真实角度反馈。

## 11. 激光 GPIO35

现有配置：

```python
LASER_IO_PIN = 35
LASER_ACTIVE_LEVEL = 1
LASER_AUTO_ON_AFTER_CAMERA = True
```

正确生命周期：

```text
初始化 GPIO35 为 OFF
→ 摄像头和媒体初始化
→ 双通道预热
→ 再打开激光
→ 异常/退出时第一时间关闭激光
```

如果改变激光模块，必须实测其 EN 是高有效还是低有效。不得默认所有模块均为高有效。

## 12. KPU 数字识别

### 12.1 模型与输入

当前防眩光模型：

```text
/sdcard/kmodel/digit_glare_int8.kmodel
输入：uint8，1×1×32×32
```

KPU 处理流程通常为：

```text
摄像头 RGB888P
→ 固定 ROI/槽位
→ 灰度、阈值、裁剪和归一化
→ 32×32
→ KPU 推理
→ 置信度/间隔门限
→ 多帧确认
```

固定左右槽位程序必须保持空槽为空，不得换成全画面自由追踪后自动补位。

### 12.2 模型资源清理

```python
del recognizer.kpu
nn.shrink_memory_pool()
gc.collect()
```

PC 上 `nncase.Simulator()` 对 K230 目标模型抛出空 `RuntimeError`，不能单独证明模型损坏；最终必须在 K230 实板加载并推理验证。

## 13. 推荐的主程序骨架

```python
sensor = None
display_ok = False

try:
    # 1. 先创建安全状态的 GPIO/UART/PWM 对象
    # 2. 创建 Sensor 并 reset
    # 3. 配置所有活跃 CAM_CHN
    # 4. 可选 Display.init
    # 5. MediaManager.init
    # 6. sensor.run
    # 7. 多通道预热
    # 8. 开激光
    # 9. 主循环：所有活跃通道持续 snapshot
    while True:
        os.exitpoint()
        rect_img = sensor.snapshot(chn=CAM_CHN_ID_0)
        laser_img = sensor.snapshot(chn=CAM_CHN_ID_1)
        # 识别、控制、有限 UART、低频显示、低频日志

except KeyboardInterrupt:
    pass
except BaseException as e:
    sys.print_exception(e)
finally:
    # 危险输出先关
    x_pwm.duty(0)
    y_pwm.duty(0)
    laser_pin.value(0)

    if isinstance(sensor, Sensor):
        sensor.stop()
    if display_ok:
        Display.deinit()
    os.exitpoint(os.EXITPOINT_ENABLE_SLEEP)
    time.sleep_ms(100)
    MediaManager.deinit()

    uart.deinit()
    x_pwm.deinit()
    y_pwm.deinit()
```

## 14. 常见错误与判断方法

| 现象 | 优先检查 |
|---|---|
| `snapshot failed(3)` | 是否每个活跃 CAM_CHN 都持续 snapshot；VB 是否未正确释放 |
| 屏幕有图、IDE 无图 | `to_ide` 是否为 False |
| 开 IDE 图传后 FPS 降低 | JPEG 压缩和 USB/IDE 传输开销，降低刷新率或关闭 |
| `Display.init`/媒体异常 | 是否在 `MediaManager.init()` 前初始化 Display |
| 下次运行媒体资源占用 | 上次是否按顺序 `sensor.stop → Display.deinit → MediaManager.deinit` |
| UART 乱码 | 两端波特率、8N1、电平、共地、TX/RX 交叉 |
| UART 二进制 CRC 全错 | 帧长、字节序、CRC 范围和接收端配置 |
| GPIO 写了但无输出 | FPIOA 是否被其他功能覆盖；是否设置为输出 |
| PWM 对象成功但 STEP 无波形 | 引脚复用、PWM 通道、duty、D36A EN、共地 |
| 两轴频率互相改变 | PWM0/1 共享时钟组 |
| 识别到目标但电机不动 | 状态机、软限位、目标频率、实际 duty、D36A EN |
| 激光 `find_blobs` 始终无候选 | 是否误用 RGB888；阈值是否针对当前 RGB565/LAB |
| 画框位置偏移 | 是否混淆检测坐标、逻辑坐标和显示偏移 |
| 跑一段时间内存不足 | 每帧分配、IDE JPEG、未释放图像/KPU、GC 策略 |

## 15. 让其他智能体修改代码前的强制检查表

要求其他智能体逐项回答：

- [ ] 已读取本文件和目标主程序。
- [ ] 已说明使用 CanMV v1.4.3，不套用 K210/OpenMV 或其他固件 API。
- [ ] 已列出所有占用的物理 IO 和 FPIOA 功能。
- [ ] 没有把摄像头通道、Sensor ID、PWM 通道和 GPIO 编号混为一谈。
- [ ] 双摄像头输出通道均持续 snapshot。
- [ ] 未改变 RGB888/RGB565 分工，或已给出实板验证依据。
- [ ] 未重复初始化 PipeLine 与 MediaManager/Display。
- [ ] 保持正确的 Sensor/Display/Media 初始化与释放顺序。
- [ ] IDE 图传和 LCD 显示是独立开关，已评估性能开销。
- [ ] UART 两端波特率、帧格式和协议完全一致。
- [ ] 未增加用户未要求的 UART 数据包。
- [ ] 已检查 PWM0/1/2 与 PWM3/4/5 的共享频率限制。
- [ ] 未把 GPIO35 同时分配给激光和 D36A EN。
- [ ] 异常退出会关闭 STEP 和激光。
- [ ] 未把软件积分位置当成编码器反馈。
- [ ] 只修改指定功能层，未顺手改变稳定识别逻辑。
- [ ] 语法检查通过，但同时明确哪些结论仍需 K230 实板验证。

## 16. 可直接复制给其他智能体的提示词

```text
你正在修改一个 K230 / CanMV MicroPython 项目。固定环境为 CanMV v1.4.3、
MicroPython e00a144、GC2093 CSI2。开始前先读取
K230_现有项目硬件与编程使用手册.md 和我指定的目标主程序。

硬件固定占用：
- GPIO42：X STEP，对应 PWM0 可选 PAD
- GPIO26：X DIR
- GPIO43：Y STEP，对应 PWM1 可选 PAD
- GPIO34：Y DIR
- GPIO32/33：UART3 TX/RX
- GPIO35：激光 TTL/EN
- D36A EN1/EN2 接驱动板自身 5V，GPIO35 不控制 D36A EN

摄像头为 Sensor(id=2) 的同一个 GC2093；CAM_CHN_ID_0 和 CHN_ID_1 是
同一 ISP 的两个输出通道，不是两只摄像头。CH0 使用 RGB888 做矩形，
CH1 使用 RGB565 做激光 find_blobs。所有启用通道每轮都必须 snapshot，
否则可能使 VB 队列积满。检测坐标、640x480 逻辑坐标和显示坐标必须显式换算。

显示初始化必须在 MediaManager.init() 前；退出顺序为 sensor.stop()、
Display.deinit()、MediaManager.deinit()。to_ide=True 会额外传 JPEG 到 IDE，
会占内存和性能。使用 PipeLine 的程序不要重复手工初始化 Sensor/Display/Media。

UART3 普通调参为 115200 8N1；Plan B 二进制链路为 460800、22 字节、
CRC16-CCITT-FALSE，必须与接收端同步，不能混用。

特别注意：PWM0/1/2 共享频率，PWM3/4/5 共享另一频率。当前 GPIO42→PWM0、
GPIO43→PWM1，因此两轴同时不同 STEP 频率存在硬件冲突。不能只因为 PWM 对象
创建成功就宣称双轴频率独立；涉及该问题必须提出实板示波器验证或重新分组方案，
未经我确认不得改接线。

只修改我指定的功能层。提交答案时给出：修改文件、修改函数、保持不变项、
资源占用变化、异常清理路径、板端验证步骤。不要把未上板验证的结果称为已验证。
```

## 17. 依据与版本边界

本手册以仓库现有代码和 CanMV v1.4 官方文档为依据：

- PWM API：`https://www.kendryte.com/k230_canmv/zh/v1.4/api/machine/K230_CanMV_PWM模块API手册.html`
- Sensor API：`https://www.kendryte.com/k230_canmv/zh/v1.4/api/mpp/K230_CanMV_Sensor模块API手册.html`
- Display API：`https://www.kendryte.com/k230_canmv/zh/v1.4/api/mpp/K230_CanMV_Display模块API手册.html`
- FPIOA API：`https://www.kendryte.com/k230_canmv/zh/v1.4/api/machine/K230_CanMV_FPIOA模块API手册.html`
- UART API：`https://www.kendryte.com/k230_canmv/zh/v1.4/api/machine/K230_CanMV_UART模块API手册.html`
- PipeLine API：`https://www.kendryte.com/k230_canmv/zh/v1.4/api/aidemo/PipeLine 模块 API 手册.html`

官方 `main` 分支文档可能包含新固件才有的接口。其他智能体查资料时必须固定到 v1.4 页面，并以本板 v1.4.3 实测结果为最终依据。
